# bankingdb-sql-project
A relational banking database built in MySQL — covering schema design, constraints, CRUD operations, filtering/sorting, aggregations, window functions, joins, subqueries, and views for reporting.
